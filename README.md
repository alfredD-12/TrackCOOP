
  # Multi-role Login System UI (Copy)

  This is a code bundle for Multi-role Login System UI (Copy). The original project is available at https://www.figma.com/design/UOS6gn7QON0dUQskLmriji/Multi-role-Login-System-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  